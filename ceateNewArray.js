//? Create a New Array : Use map() to create a new array where each numbers in the array of the numbers is doubled


let numbers = [1, 2, 3, 4, 5];

let doubleNumber = numbers.map((num) => num * 2);

console.log(doubleNumber);