//? Remove Elements: Use pop() and shift() to remove colors from the array.

let colors = ['red', 'black', 'blue', 'green'];

// colors.pop(); //pop method remove elements form last 
colors.shift();  //shift method remove element form first 

console.log(colors);